# Spring Profiles
# Default: local development (Docker Compose)
# Profiles:
#   - local: Debug logging, SQL output, dev defaults
#   - production: Optimized settings, minimal logging
#   - test: In-memory database, mocked external services
# Activation: SPRING_PROFILES_ACTIVE env var or --spring.profiles.active=profile
# Config: application-{profile}.yml in each service
