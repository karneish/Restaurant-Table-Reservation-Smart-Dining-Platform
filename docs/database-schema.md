# Database Schema (PostgreSQL 16)
# Schema auto-created by JPA Hibernate ddl-auto: update
# Tables: users, restaurants, dining_areas, restaurant_tables, table_slots,
#   reservations, reservation_tables, pre_orders, pre_order_items,
#   payments, notifications, waitlist_entries, cleaning_logs,
#   menu_items, occasion_add_ons, feedback, restaurant_ratings
# Foreign keys enforced at application level
# Indexes: Primary keys, email lookups, restaurant lookups, status filters
