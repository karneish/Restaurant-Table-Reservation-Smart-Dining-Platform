# Troubleshooting
# Services fail: Check Docker Desktop, port availability, docker compose logs
# DB connection: Verify PostgreSQL running, check credentials, wait for health check
# Gateway 503: Check Eureka registry, verify service registration
# Frontend no API: Check vite proxy config, verify Gateway on port 8080
# Cold start: Render free tier spins down, first request takes ~30s
