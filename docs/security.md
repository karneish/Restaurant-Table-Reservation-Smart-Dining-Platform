# Security Best Practices
# Auth: JWT (30min access, 7day refresh), OTP verification
# Roles: GUEST -> CUSTOMER -> STAFF -> MANAGER -> ADMIN
# Data: env vars for secrets, never commit credentials
# API: CORS, rate limiting, input validation, parameterized queries
# Docker: network isolation, health checks, non-root containers
