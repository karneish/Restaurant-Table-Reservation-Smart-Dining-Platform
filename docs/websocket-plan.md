# WebSocket Configuration (Future)
# Real-time features planned for Phase 2:
# - Live table status updates
# - Push notifications for reservation changes
# - Real-time waitlist position tracking
# - Live kitchen order status for pre-orders
# Protocol: STOMP over WebSocket
# Fallback: Server-Sent Events (SSE) for older browsers
# Configuration: spring-boot-starter-websocket dependency
